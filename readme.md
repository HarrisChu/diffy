# diffy 

简易比较客户端的工具

## 使用

配置 config.yaml，在 env 里配置环境变量，然后再 jobs 里配置命令行参数。

diffy 会启动一个子进程，调用客户端，然后收集子进程的标准输出，最后汇总信息。

## 输入

通过环境变量设置：

* Nebula 地址
* 用户名/密码
* 语句
* 并发数
* 每个并发的迭代次数

## 输出

* avg_latency_us 平均服务端响应时间
* avg_response_us 平均客户端处理时间